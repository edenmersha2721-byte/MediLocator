server:
  port: 8080

spring:
  application:
    name: api-gateway

  data:
    redis:
      host: ${SPRING_REDIS_HOST:localhost}
      port: ${SPRING_REDIS_PORT:6379}
      password: ${SPRING_REDIS_PASSWORD:}
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 10
          max-idle: 5
          min-idle: 2

  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOriginPatterns:
              - "http://localhost:3000"
              - "http://localhost:5173"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - PATCH
              - OPTIONS
            allowedHeaders: "*"
            allowCredentials: true
            maxAge: 3600

      # Redis Rate Limiter is temporarily disabled until you have a Redis server running
      # default-filters:
      #   - name: RequestRateLimiter
      #     args:
      #       redis-rate-limiter.replenishRate: 20
      #       redis-rate-limiter.burstCapacity: 40
      #       redis-rate-limiter.requestedTokens: 1

      routes:
        - id: auth-service
          uri: ${AUTH_SERVICE_URL:http://localhost:8081}
          predicates:
            - Path=/api/auth/**
          filters:
            - name: CircuitBreaker
              args:
                name: auth-service
                fallbackUri: forward:/fallback/auth

        - id: inventory-service
          uri: ${INVENTORY_SERVICE_URL:http://localhost:8082}
          predicates:
            - Path=/api/inventory/**
          # filters:
          #   - JwtAuth

        - id: search-service
          uri: ${SEARCH_SERVICE_URL:http://localhost:8083}
          predicates:
            - Path=/api/search/**
          # filters:
          #   - JwtAuth

        - id: prescription-service
          uri: ${PRESCRIPTION_SERVICE_URL:http://localhost:8084}
          predicates:
            - Path=/api/prescriptions/**
          # filters:
          #   - JwtAuth

        - id: reservation-service
          uri: ${RESERVATION_SERVICE_URL:http://localhost:8085}
          predicates:
            - Path=/api/reservations/**
          # filters:
          #   - JwtAuth

jwt:
  secret: ${JWT_SECRET:myDevSecretKeyForTesting123456789}

rate-limit:
  default-replenish-rate: 20
  default-burst-capacity: 40

# 👇 Fixed and cleanly consolidated health infrastructure settings
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized
  health:
    redis:
      enabled: false          # Turn off standard indicator
    reactive:
      redis:
        enabled: false        # Turn off reactive background indicator

logging:
  level:
    com.medicinelocator.gateway: DEBUG
    org.springframework.cloud.gateway: INFO

  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level [%X{requestId}] %logger{36} - %msg%n"